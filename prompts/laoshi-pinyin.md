---
description: Change pi-laoshi pinyin visibility
argument-hint: "on|off|hints-only"
---
Use the chinese-teacher skill. Update my pi-laoshi `pinyin_visibility` setting to `${1:-hints-only}` using `laoshi_update_settings`, then follow it in future Mandarin teaching turns.
